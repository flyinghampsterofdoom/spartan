projects:
  - name: Spartan
    environments:
      - name: Production
        services:
          - type: web
            name: spartan-operations
            runtime: node
            plan: starter
            region: oregon
            branch: main
            buildCommand: npm ci --include=dev && npm run build
            preDeployCommand: npm run db:migrate && npm run db:seed
            startCommand: npm start
            healthCheckPath: /login
            autoDeployTrigger: commit
            envVars:
              - key: NODE_ENV
                value: production
              - key: NODE_VERSION
                value: 22.13.0
              - key: DATABASE_URL
                fromDatabase:
                  name: spartan-db
                  property: connectionPoolString
              - key: APP_URL
                value: https://spartan-operations.onrender.com
              - key: SESSION_TTL_HOURS
                value: "12"
              - key: SPARTAN_BOOTSTRAP_EMAIL
                sync: false
              - key: SPARTAN_BOOTSTRAP_PASSWORD
                sync: false
              - key: EMAIL_PROVIDER
                sync: false
              - key: EMAIL_FROM
                sync: false
              - key: RESEND_API_KEY
                sync: false
        databases:
          - name: spartan-db
            plan: basic-256mb
            diskSizeGB: 1
            region: oregon
            postgresMajorVersion: "18"
            databaseName: spartan
            user: spartan
            connectionPool: pgbouncer
            storageAutoscalingEnabled: false
            ipAllowList: []
